node_modules
.next
out
*.tsbuildinfo
next-env.d.ts

# /data holds a build-time bracket snapshot + a local run store, but the
# owner-editable member_images.json must ship. Ignore data/* contents, then
# re-include the file that has to be tracked.
/data/*
!/data/member_images.json
.vercel
