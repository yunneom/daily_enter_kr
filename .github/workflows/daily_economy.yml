name: Daily — daily_economy_kr (K-경제)

# 사용 전 사전 준비:
#   1. IG 비즈니스 계정 'daily_economy_kr' 생성 + FB 페이지 연결
#   2. Meta dev 앱에서 Instagram 테스터로 등록
#   3. 토큰 발급 → exchange_token.py로 장기화
#   4. secrets 추가: INSTAGRAM_USER_ID_ECONOMY, INSTAGRAM_ACCESS_TOKEN_ECONOMY
#      (INSTAGRAM_APP_SECRET_ECONOMY는 같은 Meta 앱 쓰면 기본 _ 재사용 가능)

on:
  # 시크릿(INSTAGRAM_USER_ID_ECONOMY 등) 세팅 후 schedule 줄 주석 해제하세요.
  # schedule:
  #   - cron: '0 1 * * *'   # 한국시간 10:00 (다른 채널과 시간차)
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  CHANNEL: daily_economy_kr

jobs:
  post-news:
    runs-on: ubuntu-latest
    timeout-minutes: 110  # jitter 최대 90분 + 파이프라인 처리 여유

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Korean fonts (Pretendard + Noto fallback) + ffmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk ffmpeg unzip
          mkdir -p /tmp/pretendard
          curl -fsSL -o /tmp/pretendard.zip \
            https://github.com/orioncactus/pretendard/releases/download/v1.3.9/Pretendard-1.3.9.zip
          unzip -q -o /tmp/pretendard.zip -d /tmp/pretendard
          sudo mkdir -p /usr/share/fonts/opentype/pretendard
          sudo cp /tmp/pretendard/public/static/Pretendard-{Bold,SemiBold,Medium,Regular}.otf \
            /usr/share/fonts/opentype/pretendard/
          sudo fc-cache -f /usr/share/fonts/opentype/pretendard/

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run news posting pipeline
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          INSTAGRAM_USER_ID: ${{ secrets.INSTAGRAM_USER_ID_ECONOMY }}
          INSTAGRAM_ACCESS_TOKEN: ${{ secrets.INSTAGRAM_ACCESS_TOKEN_ECONOMY }}
          INSTAGRAM_APP_SECRET: ${{ secrets.INSTAGRAM_APP_SECRET_ECONOMY || secrets.INSTAGRAM_APP_SECRET }}
          CLOUDINARY_CLOUD_NAME: ${{ secrets.CLOUDINARY_CLOUD_NAME }}
          CLOUDINARY_UPLOAD_PRESET: ${{ secrets.CLOUDINARY_UPLOAD_PRESET }}
          UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}
        run: python main.py

      - name: Fetch insights snapshot
        if: success()
        env:
          INSTAGRAM_USER_ID: ${{ secrets.INSTAGRAM_USER_ID_ECONOMY }}
          INSTAGRAM_ACCESS_TOKEN: ${{ secrets.INSTAGRAM_ACCESS_TOKEN_ECONOMY }}
          INSIGHTS_PATH: insights_economy.json
        run: python fetch_insights.py
        continue-on-error: true

      - name: Commit state + insights
        if: always()
        run: |
          changes=""
          if [ -f state_economy.json ] && ! git diff --quiet state_economy.json 2>/dev/null; then changes="$changes state_economy.json"; fi
          if [ -f insights_economy.json ] && ! git diff --quiet insights_economy.json 2>/dev/null; then changes="$changes insights_economy.json"; fi
          if [ -z "$changes" ]; then
            echo "No changes — skipping commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add $changes
          git commit -m "state(economy): $(date -u '+%Y-%m-%d %H:%M UTC') run [skip ci]"
          git pull --rebase --autostash origin main || true
          git push

      - name: Upload generated images as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cards-economy-${{ github.run_number }}
          path: output/
          retention-days: 7
          if-no-files-found: ignore

      - name: Notify on failure
        if: failure() && env.DISCORD_WEBHOOK_URL != ''
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          payload=$(cat <<EOF
          {"content": "🚨 **daily_economy_kr 워크플로우 실패**\n\n실행 ID: \`${{ github.run_id }}\`\n로그: $RUN_URL"}
          EOF
          )
          curl -fsS -X POST -H "Content-Type: application/json" -d "$payload" "$DISCORD_WEBHOOK_URL" || echo "webhook send failed"
